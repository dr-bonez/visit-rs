[package]
name = "visit-rs"
version = "0.1.0"
edition = "2024"

[features]
default = ["serde"]

[dependencies]
async-stream = "0.3"
futures = "0.3"
visit-rs-derive = { version = "0.1.0", path = "../visit-rs-derive" }

serde = { version = "1", optional = true }

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
